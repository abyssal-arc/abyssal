{
  "name": "@abyssal/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP API, static hosting, chain feed and x402-style payments for Abyssal",
  "dependencies": {
    "@abyssal/sim": "0.1.0",
    "viem": "^2.56.8"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/api.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
